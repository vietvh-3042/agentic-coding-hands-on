# Implementer Memory Index

- [eslint airbnb + TS callback param names](eslint-airbnb-typescript-callback-param-names.md) — this repo's `no-unused-vars` false-positives on named callback-type params; established fix is `/* eslint-disable no-unused-vars */` around the interface, not renaming params.
